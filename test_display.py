import unittest
import display

class TestAppointment(unittest.TestCase):
    def func(self):
        
        self.assertEqual(self.n.config(text=str(number[self.x])), engine.say('Patient number ' + str(number[self.x]) + str(patients[self.x])) )
        self.assertEqual(self.pname.config(text=str(patients[self.x])), engine.say('Patient number ' + str(number[self.x]) + str(patients[self.x])) )

if __name__ == '__main__':
    unittest.main()